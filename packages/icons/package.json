{
  "name": "@kai/icons",
  "private": true,
  "version": "1.0.0",
  "description": "Kai UI component library icons",
  "source": "./src/index.ts",
  "main": "./dist/index.js",
  "exports": "./dist/index.modern.js",
  "module": "./dist/index.esm.js",
  "unpkg": "./dist/index.umd.js",
  "types": "./dist/types/index.d.ts",
  "typings": "./dist/types/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "clean": "rimraf dist",
    "clean:dist": "rimraf dist",
    "generate": "node ./generate.js",
    "build": "yarn clean:dist && yarn build:uncompressed",
    "build:uncompressed": "microbundle --no-compress --no-sourcemap --jsx React.createElement",
    "build:watch": "microbundle watch --no-compress --no-sourcemap --jsx React.createElement"
  },
  "peerDependencies": {
    "react": "^17.0.1"
  },
  "devDependencies": {
    "degit": "^2.8.0",
    "react": "^17.0.1"
  }
}
